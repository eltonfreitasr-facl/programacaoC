#include <stdio.h>

int main () {

/*Operadores
%% (E)
|| (ou)
!  (Não)
*/

int a = -10;

if (!(a > 0)) { //(!(a > 0)
    printf("A variável é negativa.\n");
} else {
    printf("A variável é positiva.\n");
}

}